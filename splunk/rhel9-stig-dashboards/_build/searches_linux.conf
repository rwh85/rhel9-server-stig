[stig_sro_create_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (syscall=creat OR syscall=openat OR syscall=open) success=yes \
  | rex "key=\"?(?<audit_key>[\\w\\-]+)\"?" \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | rex "auid=(?<auid>\\d+)" \
  | eval event_category="SRO", event_action="Create", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful file create syscalls on RHEL9 (SRO)

[stig_sro_create_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (syscall=creat OR syscall=openat OR syscall=open) success=no \
  | rex "key=\"?(?<audit_key>[\\w\\-]+)\"?" \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Create", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed file create syscalls on RHEL9 (SRO)

[stig_sro_access_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=access success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Access", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful unauthorized access attempts logged via auditd key=access (SRO)

[stig_sro_access_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=access success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Access", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed access attempts logged via auditd key=access (SRO)

[stig_sro_delete_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (syscall=unlink OR syscall=unlinkat OR syscall=rmdir OR syscall=rename OR syscall=renameat) success=yes \
  | rex "key=\"?(?<audit_key>[\\w\\-]+)\"?" \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Delete", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful file delete syscalls on RHEL9 (SRO)

[stig_sro_delete_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (syscall=unlink OR syscall=unlinkat OR syscall=rmdir OR syscall=rename OR syscall=renameat) success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Delete", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed file delete syscalls on RHEL9 (SRO)

[stig_sro_modify_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (key=modify OR syscall=write OR syscall=truncate OR syscall=ftruncate OR syscall=pwrite OR syscall=pwritev) success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Modify", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful file modify syscalls on RHEL9 (SRO)

[stig_sro_modify_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (key=modify OR syscall=write OR syscall=truncate OR syscall=ftruncate) success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="Modify", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed file modify syscalls on RHEL9 (SRO)

[stig_sro_perm_modify_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=perm_mod success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | rex "syscall=(?<syscall>\\S+)" \
  | eval event_category="SRO", event_action="PermissionModify", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful permission modifications (chmod/setxattr) via auditd key=perm_mod (SRO)

[stig_sro_perm_modify_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=perm_mod success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="PermissionModify", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed permission modification attempts via auditd key=perm_mod (SRO)

[stig_sro_owner_modify_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=owner_mod success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="OwnershipModify", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful ownership modifications (chown/fchown) via auditd key=owner_mod (SRO)

[stig_sro_owner_modify_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=owner_mod success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="SRO", event_action="OwnershipModify", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed ownership modification attempts via auditd key=owner_mod (SRO)

[stig_auth_logon_success_linux]
search = index=os_linux (sourcetype=linux_secure ("Accepted password" OR "Accepted publickey" OR "Accepted gssapi" OR "Accepted keyboard-interactive")) OR (sourcetype=auditd type=USER_LOGIN res=success) \
  | rex "Accepted \\S+ for (?<user>\\S+) from (?<src>\\S+)" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "addr=(?<src>\\S+)" \
  | eval event_category="Authentication", event_action="Logon", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful logon events on RHEL9 (Authentication)

[stig_auth_logon_failure_linux]
search = index=os_linux (sourcetype=linux_secure ("Failed password" OR "Failed publickey" OR "authentication failure" OR "Invalid user")) OR (sourcetype=auditd type=USER_LOGIN res=failed) \
  | rex "Failed \\S+ for (?:invalid user )?(?<user>\\S+) from (?<src>\\S+)" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "addr=(?<src>\\S+)" \
  | eval event_category="Authentication", event_action="Logon", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed logon events on RHEL9 (Authentication)

[stig_auth_logoff_success_linux]
search = index=os_linux ((sourcetype=auditd (type=USER_END OR type=USER_LOGOUT)) OR (sourcetype=linux_secure "session closed for user")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "session closed for user (?<user>\\S+)" \
  | eval event_category="Authentication", event_action="Logoff", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: logoff/session-close events on RHEL9 (Authentication). auditd does not surface logoff failures, so outcome hardcoded success.

[stig_auth_logoff_failure_linux]
search = index=os_linux sourcetype=auditd type=USER_END res=failed \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Authentication", event_action="Logoff", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: logoff failures on RHEL9 (Authentication). Rare on Linux; placeholder for USER_END res=failed.

[stig_priv_audit_policy_change_success_linux]
search = index=os_linux sourcetype=auditd (type=CONFIG_CHANGE OR type=DAEMON_CONFIG) (key=audit-policy OR key=audit-storage OR key=audit-access) res=success \
  | rex "auid=(?<auid>\\d+)" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "op=\"?(?<op>[^\\s\"]+)\"?" \
  | eval event_category="Privileged", event_action="AuditPolicyChange", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(op,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful audit policy/config changes on RHEL9 (Privileged)

[stig_priv_audit_policy_change_failure_linux]
search = index=os_linux sourcetype=auditd (type=CONFIG_CHANGE OR type=DAEMON_CONFIG) (key=audit-policy OR key=audit-storage OR key=audit-access) res=failed \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "op=\"?(?<op>[^\\s\"]+)\"?" \
  | eval event_category="Privileged", event_action="AuditPolicyChange", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(op,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed audit policy change attempts on RHEL9 (Privileged)

[stig_priv_mac_policy_change_success_linux]
search = index=os_linux sourcetype=auditd (type=MAC_POLICY_LOAD OR type=MAC_CONFIG_CHANGE OR (type=CONFIG_CHANGE key=mac-policy)) res=success \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="MACPolicyChange", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful SELinux/MAC policy changes on RHEL9 (Privileged)

[stig_priv_mac_policy_change_failure_linux]
search = index=os_linux sourcetype=auditd (type=MAC_POLICY_LOAD OR type=MAC_CONFIG_CHANGE OR (type=CONFIG_CHANGE key=mac-policy)) res=failed \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="MACPolicyChange", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed SELinux/MAC policy change attempts on RHEL9 (Privileged)

[stig_priv_config_change_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (key=sysconfig OR key=etc-passwd OR key=etc-shadow OR key=etc-group) success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | rex "key=\"?(?<audit_key>[\\w\\-]+)\"?" \
  | eval event_category="Privileged", event_action="ConfigChange", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful changes to /etc system configuration files on RHEL9 (Privileged)

[stig_priv_config_change_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL (key=sysconfig OR key=etc-passwd OR key=etc-shadow OR key=etc-group) success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="ConfigChange", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed attempts to modify /etc system configuration files on RHEL9 (Privileged)

[stig_priv_root_access_success_linux]
search = index=os_linux ((sourcetype=linux_secure "session opened for user root") OR (sourcetype=auditd type=USER_START acct="\"root\"" res=success) OR (sourcetype=auditd type=USER_LOGIN (acct="\"root\"" OR auid=0) res=success)) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval user="root", event_category="Privileged", event_action="RootAccess", source_type="linux", outcome="success", source_host=coalesce(host,src), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful interactive root/breakglass sessions on RHEL9 (Privileged)

[stig_priv_root_access_failure_linux]
search = index=os_linux ((sourcetype=linux_secure "Failed password for root") OR (sourcetype=auditd type=USER_LOGIN (acct="\"root\"" OR auid=0) res=failed)) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval user="root", event_category="Privileged", event_action="RootAccess", source_type="linux", outcome="failure", source_host=coalesce(host,src), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed root authentication attempts on RHEL9 (Privileged)

[stig_priv_escalation_success_linux]
search = index=os_linux ((sourcetype=linux_secure "sudo:" "COMMAND=") OR (sourcetype=auditd type=USER_CMD res=success) OR (sourcetype=auditd type=USER_AUTH acct="\"root\"" res=success exe="\"/usr/bin/su\"")) \
  | rex "sudo:\\s+(?<user>\\S+)\\s+:.*COMMAND=(?<cmd>[^\\r\\n]+)" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "cmd=\"(?<cmd>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="PrivilegeEscalation", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(cmd,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful privilege escalation via sudo/su on RHEL9 (Privileged)

[stig_priv_escalation_failure_linux]
search = index=os_linux ((sourcetype=linux_secure ("sudo:" ("incorrect password attempts" OR "authentication failure" OR "command not allowed" OR "user NOT in sudoers"))) OR (sourcetype=auditd type=USER_CMD res=failed) OR (sourcetype=auditd type=USER_AUTH acct="\"root\"" res=failed exe="\"/usr/bin/su\"")) \
  | rex "sudo:\\s+(?<user>\\S+)\\s+:" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "cmd=\"(?<cmd>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="PrivilegeEscalation", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(cmd,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed privilege escalation attempts via sudo/su on RHEL9 (Privileged)

[stig_priv_audit_log_access_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=audit-access success=yes \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="AuditLogAccess", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful access to /var/log/audit/* on RHEL9 (Privileged)

[stig_priv_audit_log_access_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=audit-access success=no \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="AuditLogAccess", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed access attempts to /var/log/audit/* on RHEL9 (Privileged)

[stig_priv_system_boot_success_linux]
search = index=os_linux sourcetype=auditd (type=SYSTEM_BOOT OR type=SYSTEM_SHUTDOWN OR type=DAEMON_START OR type=DAEMON_END) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="SystemBootShutdown", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(type,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: system boot/shutdown events on RHEL9 (Privileged). auditd boot/shutdown records have no outcome field; hardcoded success.

[stig_priv_system_boot_failure_linux]
search = index=os_linux sourcetype=linux_messages_syslog ("Failed to start" OR "Failed to stop" OR "shutdown failed" OR "reboot failed") \
  | rex "Failed to (?:start|stop) (?<unit>\\S+)" \
  | eval event_category="Privileged", event_action="SystemBootShutdown", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,"system"), details=coalesce(unit,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed boot/shutdown/service start-stop events on RHEL9 (Privileged)

[stig_priv_password_reset_success_linux]
search = index=os_linux ((sourcetype=auditd type=USER_CHAUTHTOK res=success) OR (sourcetype=linux_secure ("password changed for" OR "passwd:" "password updated successfully"))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "password changed for (?<user>\\S+)" \
  | eval event_category="Privileged", event_action="PasswordReset", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful password resets / account unlock on RHEL9 (Privileged)

[stig_priv_password_reset_failure_linux]
search = index=os_linux ((sourcetype=auditd type=USER_CHAUTHTOK res=failed) OR (sourcetype=linux_secure "passwd:" ("authentication token manipulation error" OR "password unchanged"))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Privileged", event_action="PasswordReset", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed password reset attempts on RHEL9 (Privileged)

[stig_account_create_success_linux]
search = index=os_linux sourcetype=auditd ((type=ADD_USER res=success) OR (type=USER_MGMT res=success op="\"adding-user\"") OR (type=SYSCALL key=usermod success=yes exe="*/useradd")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "id=(?<new_uid>\\d+)" \
  | eval event_category="Account", event_action="Create", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful user account creation on RHEL9 (Account)

[stig_account_create_failure_linux]
search = index=os_linux sourcetype=auditd ((type=ADD_USER res=failed) OR (type=USER_MGMT res=failed op="\"adding-user\"") OR (type=SYSCALL key=usermod success=no exe="*/useradd")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Create", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed user account creation attempts on RHEL9 (Account)

[stig_account_modify_success_linux]
search = index=os_linux sourcetype=auditd ((type=USER_MGMT res=success op="\"updating-user\"") OR (type=SYSCALL key=usermod success=yes (exe="*/usermod" OR exe="*/chage"))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Account", event_action="Modify", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful user account modifications via usermod/chage on RHEL9 (Account)

[stig_account_modify_failure_linux]
search = index=os_linux sourcetype=auditd ((type=USER_MGMT res=failed op="\"updating-user\"") OR (type=SYSCALL key=usermod success=no (exe="*/usermod" OR exe="*/chage"))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Modify", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed user account modification attempts on RHEL9 (Account)

[stig_account_delete_success_linux]
search = index=os_linux sourcetype=auditd ((type=DEL_USER res=success) OR (type=USER_MGMT res=success op="\"deleting-user\"") OR (type=SYSCALL key=usermod success=yes exe="*/userdel")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Delete", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful user account deletion on RHEL9 (Account)

[stig_account_delete_failure_linux]
search = index=os_linux sourcetype=auditd ((type=DEL_USER res=failed) OR (type=USER_MGMT res=failed op="\"deleting-user\"") OR (type=SYSCALL key=usermod success=no exe="*/userdel")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Delete", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed user account deletion attempts on RHEL9 (Account)

[stig_account_disable_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=usermod success=yes ((exe="*/usermod" "-L") OR (exe="*/passwd" "-l") OR (exe="*/chage" "-E" "1") OR (exe="*/chage" "-E" "0")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Account", event_action="Disable", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful account disable/lock on RHEL9 (Account)

[stig_account_disable_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=usermod success=no ((exe="*/usermod" "-L") OR (exe="*/passwd" "-l") OR (exe="*/chage" "-E")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Disable", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed account disable/lock attempts on RHEL9 (Account)

[stig_account_enable_success_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=usermod success=yes ((exe="*/usermod" "-U") OR (exe="*/passwd" "-u")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Account", event_action="Enable", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful account enable/unlock on RHEL9 (Account)

[stig_account_enable_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL key=usermod success=no ((exe="*/usermod" "-U") OR (exe="*/passwd" "-u")) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="Account", event_action="Enable", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=_raw \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed account enable/unlock attempts on RHEL9 (Account)

[stig_dta_external_media_write_success_linux]
search = index=os_linux ((sourcetype=auditd type=SYSCALL syscall=mount success=yes (key=usbdev OR name="/media/*" OR name="/run/media/*")) OR (sourcetype=auditd type=USER_DEVICE res=success key=usbdev)) \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="DTA", event_action="ExternalMediaWrite", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful writes/imports to external/removable media on RHEL9 (DTA)

[stig_dta_external_media_write_failure_linux]
search = index=os_linux sourcetype=auditd type=SYSCALL syscall=mount success=no (key=usbdev OR name="/media/*" OR name="/run/media/*") \
  | rex "name=\"?(?<path>[^\\s\"]+)\"?" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="DTA", event_action="ExternalMediaWrite", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(path,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed external/removable media mount attempts on RHEL9 (DTA)

[stig_dta_external_media_read_success_linux]
search = index=os_linux ((sourcetype=linux_messages_syslog ("kernel:" ("usb-storage" OR "USB Mass Storage" OR "Attached SCSI removable disk" OR "SCSI removable disk"))) OR (sourcetype=journald ("usb-storage" OR "Attached SCSI removable disk"))) \
  | rex "Product:\\s*(?<product>[^\\r\\n]+)" \
  | eval event_category="DTA", event_action="ExternalMediaRead", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,"system"), details=coalesce(product,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: external/removable media attach/read events from kernel messages on RHEL9 (DTA). Kernel attach events have no per-event outcome; hardcoded success.

[stig_group_create_success_linux]
search = index=os_linux sourcetype=auditd ((type=ADD_GROUP res=success) OR (type=GRP_MGMT res=success op="\"adding-group\"") OR (type=SYSCALL key=groupmod success=yes exe="*/groupadd")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Create", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful group creation on RHEL9 (GroupMgmt)

[stig_group_create_failure_linux]
search = index=os_linux sourcetype=auditd ((type=ADD_GROUP res=failed) OR (type=GRP_MGMT res=failed op="\"adding-group\"") OR (type=SYSCALL key=groupmod success=no exe="*/groupadd")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Create", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed group creation attempts on RHEL9 (GroupMgmt)

[stig_group_modify_success_linux]
search = index=os_linux sourcetype=auditd ((type=GRP_MGMT res=success op="\"updating-group\"") OR (type=SYSCALL key=groupmod success=yes exe="*/groupmod")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Modify", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful group modifications via groupmod on RHEL9 (GroupMgmt)

[stig_group_modify_failure_linux]
search = index=os_linux sourcetype=auditd ((type=GRP_MGMT res=failed op="\"updating-group\"") OR (type=SYSCALL key=groupmod success=no exe="*/groupmod")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Modify", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed group modification attempts on RHEL9 (GroupMgmt)

[stig_group_delete_success_linux]
search = index=os_linux sourcetype=auditd ((type=DEL_GROUP res=success) OR (type=GRP_MGMT res=success op="\"deleting-group\"") OR (type=SYSCALL key=groupmod success=yes exe="*/groupdel")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Delete", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful group deletion on RHEL9 (GroupMgmt)

[stig_group_delete_failure_linux]
search = index=os_linux sourcetype=auditd ((type=DEL_GROUP res=failed) OR (type=GRP_MGMT res=failed op="\"deleting-group\"") OR (type=SYSCALL key=groupmod success=no exe="*/groupdel")) \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="Delete", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed group deletion attempts on RHEL9 (GroupMgmt)

[stig_group_membership_change_success_linux]
search = index=os_linux sourcetype=auditd ((type=USER_MGMT res=success (op="\"adding-user-to-group\"" OR op="\"removing-user-from-group\"" OR op="\"adding-user-to-shadow-group\"")) OR (type=SYSCALL key=groupmod success=yes (exe="*/gpasswd" OR (exe="*/usermod" ("-G" OR "-aG" OR "-g"))))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | rex "op=\"(?<op>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="MembershipChange", source_type="linux", outcome="success", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(op,grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful additions/removals of users to/from groups on RHEL9 (GroupMgmt)

[stig_group_membership_change_failure_linux]
search = index=os_linux sourcetype=auditd ((type=USER_MGMT res=failed (op="\"adding-user-to-group\"" OR op="\"removing-user-from-group\"")) OR (type=SYSCALL key=groupmod success=no (exe="*/gpasswd" OR (exe="*/usermod" ("-G" OR "-aG"))))) \
  | rex "acct=\"(?<acct>[^\"]+)\"" \
  | rex "grp=\"(?<grp>[^\"]+)\"" \
  | eval event_category="GroupMgmt", event_action="MembershipChange", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,acct,name,src_user,"unknown"), details=coalesce(grp,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed attempts to modify group membership on RHEL9 (GroupMgmt)

[stig_health_audit_storage_warning_linux]
search = index=os_linux ((sourcetype=auditd (type=DAEMON_RESOURCE OR (type=DAEMON_END reason="\"disk-full\""))) OR (sourcetype=linux_messages_syslog ("auditd:" ("disk_full_action" OR "audit_log_full" OR "remaining space is low" OR "No space left on device")))) \
  | rex "reason=\"(?<reason>[^\"]+)\"" \
  | eval event_category="Health", event_action="AuditStorage75", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,"auditd"), details=coalesce(reason,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: audit storage volume warnings (75%/full) and disk-pressure DAEMON_END on RHEL9 (Health)

[stig_health_audit_processing_failure_linux]
search = index=os_linux ((sourcetype=auditd (type=DAEMON_ABORT OR (type=ANOM_ABEND exe="*/auditd"))) OR (sourcetype=linux_messages_syslog ("kernel: audit: backlog limit exceeded" OR "kernel: audit: audit_lost" OR "audit: lost messages" OR "auditd: Audit daemon log"))) \
  | rex "reason=\"(?<reason>[^\"]+)\"" \
  | rex "exe=\"(?<exe>[^\"]+)\"" \
  | eval event_category="Health", event_action="AuditProcessingFailure", source_type="linux", outcome="failure", source_host=coalesce(host,src), user=coalesce(user,"auditd"), details=coalesce(reason,exe,_raw) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: audit subsystem processing failures (daemon abort, abend, backlog/lost messages) on RHEL9 (Health)

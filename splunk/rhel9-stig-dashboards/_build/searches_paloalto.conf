[stig_auth_logon_success_pan_admin]
search = index=network sourcetype=pan:system subtype=auth event_id="auth-success*" \
  | eval event_category="Authentication", event_action="Admin Logon", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful admin console logon on Palo Alto (Authentication)

[stig_auth_logon_fail_pan_admin]
search = index=network sourcetype=pan:system subtype=auth event_id="auth-fail*" \
  | eval event_category="Authentication", event_action="Admin Logon", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed admin console logon on Palo Alto (Authentication)

[stig_auth_logoff_pan_admin]
search = index=network sourcetype=pan:system subtype=auth event_id="auth-logout*" \
  | eval event_category="Authentication", event_action="Admin Logoff", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: admin console logoff on Palo Alto (Authentication)

[stig_auth_logon_success_pan_gp]
search = (index=network sourcetype=pan:globalprotect event_id="globalprotectgateway-login-succ*") OR (index=network sourcetype=pan:system event_id="globalprotectgateway-login-succ*") \
  | eval event_category="Authentication", event_action="GlobalProtect Logon", source_type="paloalto", outcome="success", source_host=host, user=coalesce(user,admin,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful GlobalProtect VPN logon (Authentication)

[stig_auth_logon_fail_pan_gp]
search = (index=network sourcetype=pan:globalprotect event_id="globalprotectgateway-login-fail*") OR (index=network sourcetype=pan:system event_id="globalprotectgateway-login-fail*") \
  | eval event_category="Authentication", event_action="GlobalProtect Logon", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(user,admin,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed GlobalProtect VPN logon (Authentication)

[stig_auth_logoff_pan_gp]
search = (index=network sourcetype=pan:globalprotect event_id="globalprotectgateway-logout-succ*") OR (index=network sourcetype=pan:system event_id="globalprotectgateway-logout-succ*") \
  | eval event_category="Authentication", event_action="GlobalProtect Logoff", source_type="paloalto", outcome="success", source_host=host, user=coalesce(user,admin,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: GlobalProtect VPN logoff (Authentication)

[stig_priv_config_commit_success_pan]
search = (index=network sourcetype=pan:config result="Succeeded") OR (index=network sourcetype=pan:system subtype=general event_desc="*commit succeeded*") \
  | eval event_category="Privileged", event_action="Config Commit", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,cmd,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful PAN-OS configuration commit (Privileged)

[stig_priv_config_commit_fail_pan]
search = (index=network sourcetype=pan:config result="Failed") OR (index=network sourcetype=pan:system subtype=general event_desc="*commit failed*") \
  | eval event_category="Privileged", event_action="Config Commit", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,cmd,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS configuration commit (Privileged)

[stig_priv_policy_change_success_pan]
search = index=network sourcetype=pan:config cmd IN (set,delete,edit,rename,move) (path="*rulebase security*" OR path="*network*" OR path="*device setting*" OR path="*log-settings*") result="Submitted" \
  | eval event_category="Privileged", event_action="Security/Audit Policy Change", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful security/audit policy change on Palo Alto (Privileged)

[stig_priv_policy_change_fail_pan]
search = index=network sourcetype=pan:config cmd IN (set,delete,edit,rename,move) (path="*rulebase security*" OR path="*network*" OR path="*device setting*" OR path="*log-settings*") result="Failed" \
  | eval event_category="Privileged", event_action="Security/Audit Policy Change", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed security/audit policy change on Palo Alto (Privileged)

[stig_priv_system_reboot_pan]
search = index=network sourcetype=pan:system subtype=general (event_desc="*reboot*" OR event_desc="*system started*" OR event_desc="*pan_init*") \
  | eval event_category="Privileged", event_action="System Reboot/Restart", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS system reboot/restart (Privileged)

[stig_priv_password_reset_pan]
search = index=network sourcetype=pan:system event_id="mgmt-admin-password-changed" \
  | eval event_category="Privileged", event_action="Admin Password Reset", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin password reset (Privileged)

[stig_priv_account_lockout_pan]
search = index=network sourcetype=pan:system event_id="mgmt-admin-account-locked" \
  | eval event_category="Privileged", event_action="Admin Account Lockout", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account locked (Privileged)

[stig_priv_account_unlock_pan]
search = index=network sourcetype=pan:system event_id="mgmt-admin-account-unlocked" \
  | eval event_category="Privileged", event_action="Admin Account Unlock", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account unlock (Privileged)

[stig_account_create_success_pan]
search = index=network sourcetype=pan:config cmd=set path="*mgt-config users *" NOT path="*disabled*" NOT path="*role-based*" NOT path="*password*" result="Submitted" \
  | eval event_category="Account", event_action="Admin Account Create/Modify", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account create/modify (Account)

[stig_account_create_fail_pan]
search = index=network sourcetype=pan:config cmd=set path="*mgt-config users *" NOT path="*disabled*" NOT path="*role-based*" NOT path="*password*" result="Failed" \
  | eval event_category="Account", event_action="Admin Account Create/Modify", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS admin account create/modify (Account)

[stig_account_delete_success_pan]
search = index=network sourcetype=pan:config cmd=delete path="*mgt-config users *" result="Submitted" \
  | eval event_category="Account", event_action="Admin Account Delete", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account delete (Account)

[stig_account_delete_fail_pan]
search = index=network sourcetype=pan:config cmd=delete path="*mgt-config users *" result="Failed" \
  | eval event_category="Account", event_action="Admin Account Delete", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS admin account delete (Account)

[stig_account_disable_success_pan]
search = index=network sourcetype=pan:config cmd=set path="*mgt-config users *" (path="*disabled yes*" OR after_change_detail="*disabled*yes*") result="Submitted" \
  | eval event_category="Account", event_action="Admin Account Disable", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,after_change_detail,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account disable (Account)

[stig_account_enable_success_pan]
search = index=network sourcetype=pan:config cmd=delete path="*mgt-config users *disabled*" result="Submitted" \
  | eval event_category="Account", event_action="Admin Account Enable", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,before_change_detail,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin account enable (Account)

[stig_dta_config_export_success_pan]
search = (index=network sourcetype=pan:system event_id="config-export*" result="Succeeded") OR (index=network sourcetype=pan:system event_desc="*Saved configuration*") \
  | eval event_category="DTA", event_action="Config Export/Save", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful PAN-OS config export/save (DTA)

[stig_dta_config_export_fail_pan]
search = index=network sourcetype=pan:system event_id="config-export*" result="Failed" \
  | eval event_category="DTA", event_action="Config Export/Save", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS config export/save (DTA)

[stig_dta_config_import_success_pan]
search = (index=network sourcetype=pan:system event_id="config-import*" result="Succeeded") OR (index=network sourcetype=pan:system event_desc="*Loaded configuration from*") \
  | eval event_category="DTA", event_action="Config Import/Load", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: successful PAN-OS config import/load (DTA)

[stig_dta_config_import_fail_pan]
search = index=network sourcetype=pan:system event_id="config-import*" result="Failed" \
  | eval event_category="DTA", event_action="Config Import/Load", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS config import/load (DTA)

[stig_groupmgmt_role_create_success_pan]
search = index=network sourcetype=pan:config cmd=set (path="*mgt-config admin-role *" OR path="*device admin-role *") result="Submitted" \
  | eval event_category="GroupMgmt", event_action="Admin Role Create/Modify", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin role create/modify (GroupMgmt)

[stig_groupmgmt_role_create_fail_pan]
search = index=network sourcetype=pan:config cmd=set (path="*mgt-config admin-role *" OR path="*device admin-role *") result="Failed" \
  | eval event_category="GroupMgmt", event_action="Admin Role Create/Modify", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS admin role create/modify (GroupMgmt)

[stig_groupmgmt_role_delete_success_pan]
search = index=network sourcetype=pan:config cmd=delete (path="*mgt-config admin-role *" OR path="*device admin-role *") result="Submitted" \
  | eval event_category="GroupMgmt", event_action="Admin Role Delete", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS admin role delete (GroupMgmt)

[stig_groupmgmt_role_delete_fail_pan]
search = index=network sourcetype=pan:config cmd=delete (path="*mgt-config admin-role *" OR path="*device admin-role *") result="Failed" \
  | eval event_category="GroupMgmt", event_action="Admin Role Delete", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS admin role delete (GroupMgmt)

[stig_groupmgmt_user_role_assign_success_pan]
search = index=network sourcetype=pan:config cmd IN (set,delete) path="*mgt-config users *role-based*" result="Submitted" \
  | eval event_category="GroupMgmt", event_action="User-to-Role Mapping Change", source_type="paloalto", outcome="success", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS user-to-role mapping change (GroupMgmt)

[stig_groupmgmt_user_role_assign_fail_pan]
search = index=network sourcetype=pan:config cmd IN (set,delete) path="*mgt-config users *role-based*" result="Failed" \
  | eval event_category="GroupMgmt", event_action="User-to-Role Mapping Change", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(cmd." ".path,event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: failed PAN-OS user-to-role mapping change (GroupMgmt)

[stig_health_disk_usage_pan]
search = index=network sourcetype=pan:system subtype=general severity IN (high,critical,medium) (event_desc="*log partition*" OR event_desc="*disk usage*" OR event_desc="*disk space*") \
  | eval event_category="Health", event_action="Disk/Log Partition Usage Warning", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS disk/log partition usage warning (Health)

[stig_health_log_forwarding_fail_pan]
search = index=network sourcetype=pan:system subtype=general (event_desc="*log forwarding failed*" OR event_desc="*syslog*fail*" OR event_desc="*unable to forward*") \
  | eval event_category="Health", event_action="Log Forwarding Failure", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS log forwarding failure (Health)

[stig_health_audit_processing_fail_pan]
search = index=network sourcetype=pan:system subtype=general severity IN (high,critical) (event_desc="*log-receiver*" OR event_desc="*logd*" OR event_desc="*log daemon*" OR event_desc="*log writing*fail*") \
  | eval event_category="Health", event_action="Audit/Log Processing Failure", source_type="paloalto", outcome="fail", source_host=host, user=coalesce(admin,user,"unknown"), details=substr(coalesce(event_desc,_raw),1,512) \
  | table _time, event_category, event_action, source_host, source_type, outcome, user, details
disabled = 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
description = STIG: PAN-OS audit/log processing failure (Health)
